<?xml version="1.0" encoding="utf-8" ?>
<ContentView x:Class="MyCraftyStash.Views.InventoryView"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:MyCraftyStash.ViewModels"
             xmlns:models="clr-namespace:MyCraftyStash.Models"
             x:Name="Root"
             x:DataType="vm:InventoryViewModel">

    <Grid RowDefinitions="Auto,*">

        <!-- Header + search + filters -->
        <VerticalStackLayout Grid.Row="0" Spacing="0" Padding="24,18,24,8">
            <Label Text="My Crafty Stash  /  Inventory" Style="{StaticResource Breadcrumb}"/>
            <Label Text="Inventory" Style="{StaticResource PageTitle}" Margin="0,4,0,0"/>
            <Label Text="Manage your craft supplies — search, sort, and add new items."
                   Style="{StaticResource PageSubtitle}"/>

            <!-- Toolbar: everything aligned on one row, uniform 42px height.
                 The button columns use FIXED widths (not Auto) so the dropdown
                 overlay grid can reproduce the exact same column layout and
                 anchor each list directly under its button. -->
            <Grid ColumnDefinitions="*,168,168,104,84,108,124" ColumnSpacing="10" Margin="0,14,0,0"
                  VerticalOptions="Start">
                <!-- Search -->
                <Border Grid.Column="0" BackgroundColor="{StaticResource StashInput}" Stroke="{StaticResource StashBorder}"
                        StrokeThickness="1" StrokeShape="RoundRectangle 8" Padding="12,0" HeightRequest="42" VerticalOptions="Start">
                    <Entry Text="{Binding SearchText}" Placeholder="Search your stash" ReturnCommand="{Binding SearchCommand}"
                           BackgroundColor="Transparent" TextColor="{StaticResource StashText}" VerticalOptions="Center"/>
                </Border>
                <!-- Type dropdown (custom: value always shown + ▾) -->
                <Border Grid.Column="1" BackgroundColor="{StaticResource StashInput}" Stroke="{StaticResource StashBorder}"
                        StrokeThickness="1" StrokeShape="RoundRectangle 8" HeightRequest="42" VerticalOptions="Start">
                    <Border.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding ToggleTypeDropdownCommand}"/>
                    </Border.GestureRecognizers>
                    <Grid ColumnDefinitions="*,Auto" Padding="12,0">
                        <Label Text="{Binding SelectedType}" TextColor="{StaticResource StashText}" FontSize="14"
                               VerticalOptions="Center" LineBreakMode="TailTruncation"/>
                        <Label Grid.Column="1" Text="▾" TextColor="{StaticResource StashTextMuted}" FontSize="16" VerticalOptions="Center"/>
                    </Grid>
                </Border>
                <!-- Search-scope dropdown (replaces the checkboxes) -->
                <Border Grid.Column="2" BackgroundColor="{StaticResource StashInput}" Stroke="{StaticResource StashBorder}"
                        StrokeThickness="1" StrokeShape="RoundRectangle 8" HeightRequest="42" VerticalOptions="Start">
                    <Border.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding ToggleScopeDropdownCommand}"/>
                    </Border.GestureRecognizers>
                    <Grid ColumnDefinitions="*,Auto" Padding="12,0">
                        <Label Text="{Binding SearchScope}" TextColor="{StaticResource StashText}" FontSize="14"
                               VerticalOptions="Center" LineBreakMode="TailTruncation"/>
                        <Label Grid.Column="1" Text="▾" TextColor="{StaticResource StashTextMuted}" FontSize="16" VerticalOptions="Center"/>
                    </Grid>
                </Border>
                <!-- Search / Clear / No image / Add -->
                <Button Grid.Column="3" Text="Search" Command="{Binding SearchCommand}" Style="{StaticResource PrimaryButton}"
                        HeightRequest="42" Padding="18,0" VerticalOptions="Start" HorizontalOptions="Fill"/>
                <Button Grid.Column="4" Text="Clear" Command="{Binding ClearFiltersCommand}" Style="{StaticResource SecondaryButton}"
                        HeightRequest="42" Padding="16,0" VerticalOptions="Start" HorizontalOptions="Fill"/>
                <Button Grid.Column="5" Text="No image" Command="{Binding ToggleNoImageCommand}" Style="{StaticResource SecondaryButton}"
                        HeightRequest="42" Padding="16,0" VerticalOptions="Start" HorizontalOptions="Fill">
                    <Button.Triggers>
                        <DataTrigger TargetType="Button" Binding="{Binding NoImageOnly}" Value="True">
                            <Setter Property="BackgroundColor" Value="{StaticResource StashPrimary}"/>
                            <Setter Property="TextColor" Value="White"/>
                        </DataTrigger>
                    </Button.Triggers>
                </Button>
                <Button Grid.Column="6" Text="+ Add Item" Command="{Binding AddItemCommand}" Style="{StaticResource PrimaryButton}"
                        HeightRequest="42" Padding="18,0" VerticalOptions="Start" HorizontalOptions="Fill"/>
            </Grid>

            <!-- Subtype filter checkboxes (appear when a type is selected) -->
            <FlexLayout Wrap="Wrap" Margin="0,10,0,0" IsVisible="{Binding HasSubtypeFilters}"
                        BindableLayout.ItemsSource="{Binding SubtypeFilters}">
                <BindableLayout.ItemTemplate>
                    <DataTemplate x:DataType="vm:CheckOption">
                        <HorizontalStackLayout Spacing="2" Margin="0,0,16,4">
                            <CheckBox IsChecked="{Binding IsSelected}" HeightRequest="22" WidthRequest="22"/>
                            <Label Text="{Binding Label}" VerticalOptions="Center" FontSize="12" TextColor="{StaticResource StashText}"/>
                        </HorizontalStackLayout>
                    </DataTemplate>
                </BindableLayout.ItemTemplate>
            </FlexLayout>
        </VerticalStackLayout>

        <RefreshView Grid.Row="1" Command="{Binding DoRefreshCommand}" IsRefreshing="{Binding IsRefreshing}">
            <CollectionView ItemsSource="{Binding Items}" SelectionMode="None" Margin="16,0">
                <CollectionView.ItemsLayout>
                    <GridItemsLayout Orientation="Vertical" Span="3"
                                     HorizontalItemSpacing="12" VerticalItemSpacing="12"/>
                </CollectionView.ItemsLayout>
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="models:Item">
                        <Border StrokeShape="RoundRectangle 12" StrokeThickness="1" Stroke="{StaticResource StashBorder}"
                                BackgroundColor="{StaticResource StashCard}" Padding="10" HeightRequest="238">
                            <Border.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding BindingContext.OpenItemCommand, Source={x:Reference Root}, x:DataType=ContentView}"
                                                      CommandParameter="{Binding .}"/>
                            </Border.GestureRecognizers>
                            <!-- Fixed-height card: photo fills the top, name/theme pinned to the bottom -->
                            <Grid RowDefinitions="*,Auto" RowSpacing="8">
                                <!-- Photo + overlaid badges (white backdrop hides pillarbox bars) -->
                                <Grid Grid.Row="0">
                                    <Border StrokeShape="RoundRectangle 8" StrokeThickness="0" BackgroundColor="White">
                                        <Image Source="{Binding ImageUrl, Converter={StaticResource ImageStringConverter}}" Aspect="AspectFit"/>
                                    </Border>
                                    <!-- Subtype/type badge (top-left) -->
                                    <Border BackgroundColor="{StaticResource StashBadge}" StrokeThickness="0" StrokeShape="RoundRectangle 4"
                                            Padding="6,2" HorizontalOptions="Start" VerticalOptions="Start" Margin="8">
                                        <Label Text="{Binding TypeSubtypeBadge}" FontSize="10" FontAttributes="Bold" TextColor="{StaticResource StashText}"/>
                                    </Border>
                                    <!-- Layers badge (bottom-right, stencils) -->
                                    <Border BackgroundColor="{StaticResource StashPrimary}" StrokeThickness="0" StrokeShape="RoundRectangle 4"
                                            Padding="6,2" HorizontalOptions="End" VerticalOptions="End" Margin="8"
                                            IsVisible="{Binding ShowLayersBadge}">
                                        <Label Text="{Binding LayersText}" FontSize="10" FontAttributes="Bold" TextColor="White"/>
                                    </Border>
                                    <!-- Discontinued badge (bottom-left) -->
                                    <Border BackgroundColor="{StaticResource ErrorRed}" StrokeThickness="0" StrokeShape="RoundRectangle 4"
                                            Padding="6,2" HorizontalOptions="Start" VerticalOptions="End" Margin="8"
                                            IsVisible="{Binding IsDiscontinued}">
                                        <Label Text="Discontinued" FontSize="10" FontAttributes="Bold" TextColor="White"/>
                                    </Border>
                                </Grid>
                                <!-- Name + theme (left) and stock badge (right) -->
                                <Grid Grid.Row="1" ColumnDefinitions="*,Auto" ColumnSpacing="6">
                                    <VerticalStackLayout Grid.Column="0" Spacing="1" VerticalOptions="End">
                                        <Label Text="{Binding Name}" FontAttributes="Bold" FontSize="14"
                                               TextColor="{StaticResource StashText}" MaxLines="2" LineBreakMode="TailTruncation"/>
                                        <Label Text="{Binding Theme}" FontSize="12" TextColor="{StaticResource StashPrimary}"
                                               MaxLines="1" LineBreakMode="TailTruncation" IsVisible="{Binding HasTheme}"/>
                                    </VerticalStackLayout>
                                    <Border Grid.Column="1" BackgroundColor="{StaticResource StashSidebar}" StrokeThickness="0"
                                            StrokeShape="RoundRectangle 4" Padding="6,2" VerticalOptions="End"
                                            IsVisible="{Binding HasStock}">
                                        <Label Text="{Binding StockText}" FontSize="11" FontAttributes="Bold" TextColor="{StaticResource StashText}"/>
                                    </Border>
                                </Grid>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
                <CollectionView.EmptyView>
                    <VerticalStackLayout Spacing="10" VerticalOptions="Center" Padding="40">
                        <Label Text="Nothing here yet" FontSize="20" FontAttributes="Bold"
                               HorizontalOptions="Center" TextColor="{StaticResource StashText}"/>
                        <Label Text="Tap Add Item to put your first item in the stash, or adjust your search."
                               HorizontalTextAlignment="Center" TextColor="{StaticResource StashTextMuted}"/>
                    </VerticalStackLayout>
                </CollectionView.EmptyView>
            </CollectionView>
        </RefreshView>

        <ActivityIndicator Grid.Row="1" IsRunning="{Binding Busy}" IsVisible="{Binding Busy}"
                           VerticalOptions="Center" HorizontalOptions="Center"/>

        <!-- Dropdown overlay: transparent (tap outside to close); lists anchored
             under their trigger via a column grid that mirrors the toolbar -->
        <Grid Grid.RowSpan="2" IsVisible="{Binding AnyDropdownOpen}" BackgroundColor="Transparent">
            <Grid.GestureRecognizers>
                <TapGestureRecognizer Command="{Binding CloseDropdownsCommand}"/>
            </Grid.GestureRecognizers>
            <Grid ColumnDefinitions="*,168,168,104,84,108,124" ColumnSpacing="10" Padding="24,0"
                  VerticalOptions="Start" Margin="0,152,0,0">
                <!-- Type list, under the type button (col 1) -->
                <Border Grid.Column="1" IsVisible="{Binding TypeDropdownOpen}" WidthRequest="240" HorizontalOptions="Start"
                        MaximumHeightRequest="420" BackgroundColor="{StaticResource StashCard}" Stroke="{StaticResource StashBorder}"
                        StrokeThickness="1" StrokeShape="RoundRectangle 10" Padding="6">
                    <ScrollView MaximumHeightRequest="408">
                        <VerticalStackLayout BindableLayout.ItemsSource="{Binding Types}">
                            <BindableLayout.ItemTemplate>
                                <DataTemplate x:DataType="x:String">
                                    <Border BackgroundColor="Transparent" StrokeThickness="0" StrokeShape="RoundRectangle 6" Padding="12,9">
                                        <Border.GestureRecognizers>
                                            <TapGestureRecognizer Command="{Binding BindingContext.SelectTypeCommand, Source={x:Reference Root}, x:DataType=ContentView}"
                                                                  CommandParameter="{Binding .}"/>
                                        </Border.GestureRecognizers>
                                        <Label Text="{Binding .}" FontSize="14" TextColor="{StaticResource StashText}"/>
                                    </Border>
                                </DataTemplate>
                            </BindableLayout.ItemTemplate>
                        </VerticalStackLayout>
                    </ScrollView>
                </Border>
                <!-- Scope list, under the scope button (col 2) -->
                <Border Grid.Column="2" IsVisible="{Binding ScopeDropdownOpen}" WidthRequest="200" HorizontalOptions="Start"
                        BackgroundColor="{StaticResource StashCard}" Stroke="{StaticResource StashBorder}"
                        StrokeThickness="1" StrokeShape="RoundRectangle 10" Padding="6">
                    <VerticalStackLayout BindableLayout.ItemsSource="{Binding ScopeOptions}">
                        <BindableLayout.ItemTemplate>
                            <DataTemplate x:DataType="x:String">
                                <Border BackgroundColor="Transparent" StrokeThickness="0" StrokeShape="RoundRectangle 6" Padding="12,9">
                                    <Border.GestureRecognizers>
                                        <TapGestureRecognizer Command="{Binding BindingContext.SelectScopeCommand, Source={x:Reference Root}, x:DataType=ContentView}"
                                                              CommandParameter="{Binding .}"/>
                                    </Border.GestureRecognizers>
                                    <Label Text="{Binding .}" FontSize="14" TextColor="{StaticResource StashText}"/>
                                </Border>
                            </DataTemplate>
                        </BindableLayout.ItemTemplate>
                    </VerticalStackLayout>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</ContentView>
